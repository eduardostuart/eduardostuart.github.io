<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>
      {%block title %}Eduardo Stuart{% endblock %}
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,400;1,500&amp;display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ '/style.css' | url }}?v={{ siteVersion }}"/>
  </head>

  <body class="font-mono bg-gradient-to-b from-slate-100 to-slate-50 dark:from-black dark:to-gray-900">
    <main>
      <div class="w-full max-w-screen-lg pt-7 sm:pt-14 pb-7 mx-auto px-7 lg:px-6">

        <header class="flex flex-row flex-wrap sm:flex-nowrap items-center w-full border-b border-gray-100 dark:border-white/10 py-4">
          <a href="{{ '/' | url }}" class="text-2xl no-underline text-black dark:text-white font-extrabold">
            <h1>
                  Eduardo Stuart
              </h1>
          </a>

          <div class="block w-full sm:ml-auto sm:w-auto text-gray-400">
            {% block current_page %}{% endblock %}
          </div>

        </header>

        <section class="w-full text-gray-700 dark:text-white">
          {% block body %}{% endblock %}
        </section>
      </div>
    </main>
  </body>
</html>